# 博客可视化管理工具

## 启动

最简单的方法是直接双击博客目录里的 `启动博客管理工具.bat`，浏览器会自动打开管理界面。关闭随之出现的命令窗口即可停止工具。

也可以在博客文件夹中打开 PowerShell，手动执行：

```powershell
npm run admin
```

看到地址后，在浏览器打开：

```text
http://127.0.0.1:4173/admin/
```

## 使用方法

1. 在左侧选择要修改的页面。
2. 直接点击中间预览里的标题、文字、图片或卡片。
3. 在右侧修改文字、图片地址、链接或卡片尺寸。
4. 可以复制或删除选中的卡片，也可以用页面栏旁的 `＋` 新建页面。
5. 点击右上角“保存修改”。

对于主页进入动画、游戏分类等需要点击才能显示的内容，先点击预览顶部的“编辑模式”切换为“交互模式”。完成页面内操作后，再切回“编辑模式”点选下一级内容。

每次保存前，工具都会把原页面复制到 `.admin-backups` 文件夹。这个工具只监听本机地址，不会对公网开放。

## 注意

- 修改图片时可以填写网络地址，也可以填写博客目录内图片的相对路径。
- “预览页面”会在新标签页打开当前页面。
- 管理工具运行期间，不要同时用其他编辑器保存同一个 HTML 文件。
